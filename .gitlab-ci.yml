stages:
  - build
  - test

build-job:
  stage: build
  image: python:3.12-slim
  script:
    - echo "Hello, $GITLAB_USER_LOGIN!"

test-job1:
  stage: test
  image: python:3.12-slim
  script:
      pip install -r requirements.txt
      pytest